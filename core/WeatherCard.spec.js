import { describe, test } from '@jest/globals'
import { WeatherCard } from './WeatherCard.js'

describe('WeatherCard', () => {
  test('instantiating a weather card', () => {
    const weatherCard = new WeatherCard(0)
  })
})
