import { describe, test } from '@jest/globals'
import { DiscardPile } from './DiscardPile.js'

describe('DiscardPile', () => {
  test('instantiating a discard pile', () => {
    const discardPile = new DiscardPile()
  })
})
